export type SignupArgs = {
    email: string;
    password: string;
    fullName: string;
};

export type SignupFormFields = {
    email: string;
    password: string;
    passwordConfirm: string;
    fullName: string;
};
